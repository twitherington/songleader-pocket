<h1>New Song</h1>

<%= form_for @song do |f| %>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </p>

    <p>
      <%= f.label :song_number %><br>
      <%= f.text_field :song_number %>
    </p>

    <p>
      <%= f.label :pitches %><br>
      <%= f.select :pitch_id, Pitch.all.collect { |p| [ p.name, p.id ] }, include_blank: true %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>
